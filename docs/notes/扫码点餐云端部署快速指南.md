# 扫码点餐云端部署快速指南

## 🎯 目标

将扫码点餐功能部署到 uniCloud，让小程序可以生成真实的小程序码。

## ⏱️ 预计时间

30-60 分钟（首次部署）

---

## 📋 前置准备

### 1. 确认已安装
- ✅ HBuilderX（最新版本）
- ✅ 微信开发者工具

### 2. 确认已有账号
- ✅ DCloud 账号（用于 uniCloud）
- ✅ 微信小程序账号（已获取 AppID 和 AppSecret）

### 3. 当前凭证信息
```
AppID:     wxc63d199886077877
AppSecret: e51393d39a5e8515bc97542eca91f74b
```

---

## 🚀 部署步骤

### 第一步：创建云服务空间（5分钟）

1. **打开 HBuilderX**
   - 打开项目：`私房菜点餐项目前端模版（微信小程序+H5）`

2. **右键点击 `uniCloud-aliyun` 目录**
   - 选择：`创建云服务空间或绑定已有服务空间`

3. **创建新的服务空间**
   - 点击：`创建新的服务空间`
   - 服务商：选择 `阿里云`
   - 空间名称：输入 `qiyun-restaurant`（或其他名称）
   - 点击：`创建`

4. **等待创建完成**
   - 大约需要 1-2 分钟
   - 创建成功后会自动绑定到项目

---

### 第二步：配置微信凭证（5分钟）

#### 方法 A：使用 Web 控制台（推荐）

1. **打开 uniCloud Web 控制台**
   - 右键点击 `uniCloud-aliyun` 目录
   - 选择：`打开 uniCloud Web 控制台`
   - 会在浏览器中打开

2. **进入配置页面**
   - 点击左侧菜单：`云函数/云对象`
   - 点击顶部标签：`公共模块或扩展库`
   - 找到或新建：`uni-open-bridge`

3. **添加配置**
   ```json
   {
     "mp-weixin": {
       "appid": "wxc63d199886077877",
       "appsecret": "e51393d39a5e8515bc97542eca91f74b"
     }
   }
   ```
   - 点击：`保存`

#### 方法 B：使用环境变量（备选）

1. **在控制台找到云对象**
   - 点击：`云函数/云对象`
   - 找到：`zhuohao` 云对象

2. **添加环境变量**
   - 点击：`云函数配置` 或 `环境变量`
   - 添加两个变量：
     ```
     WX_APPID = wxc63d199886077877
     WX_APPSECRET = e51393d39a5e8515bc97542eca91f74b
     ```
   - 保存

---

### 第三步：创建数据库集合（10分钟）

1. **打开数据库管理**
   - 在 Web 控制台，点击左侧：`云数据库`
   - 点击：`新建集合`

2. **创建 `zhuohao` 集合**
   - 集合名称：`zhuohao`
   - 点击：`确定`

3. **设置索引**
   - 点击 `zhuohao` 集合
   - 点击：`索引管理`
   - 点击：`新建索引`
   - 配置：
     ```
     索引字段：table_number
     索引类型：唯一索引
     排序方式：升序
     ```
   - 点击：`确定`

4. **创建 `token_cache` 集合**
   - 重复上述步骤
   - 集合名称：`token_cache`
   - 索引字段：`cache_key`（唯一索引）

5. **创建 `error_logs` 集合**
   - 集合名称：`error_logs`
   - 不需要设置索引

---

### 第四步：配置云存储（5分钟）

1. **打开云存储管理**
   - 在 Web 控制台，点击左侧：`云存储`

2. **创建目录**
   - 点击：`新建目录`
   - 目录名称：`qrcodes`
   - 点击：`确定`

3. **设置权限（可选）**
   - 点击 `qrcodes` 目录
   - 点击：`权限设置`
   - 读权限：`所有用户可读`
   - 写权限：`仅创建者可写`

---

### 第五步：上传云对象（5分钟）

1. **在 HBuilderX 中找到云对象**
   - 展开：`uniCloud-aliyun/cloudfunctions/zhuohao`

2. **右键点击 `zhuohao` 目录**
   - 选择：`上传部署`
   - 或选择：`上传并运行`

3. **等待上传完成**
   - 控制台会显示上传进度
   - 上传成功后会显示：`上传成功`

---

### 第六步：测试云对象（5分钟）

1. **打开云函数测试**
   - 右键点击 `zhuohao` 云对象
   - 选择：`运行-云端测试`

2. **测试生成小程序码**
   - 在测试窗口输入：
     ```javascript
     {
       "method": "generateQRCode",
       "params": ["测试桌1"]
     }
     ```
   - 点击：`运行`

3. **查看结果**
   - 如果返回：
     ```json
     {
       "success": true,
       "data": {
         "id": "...",
         "qrcodeUrl": "...",
         "tableNumber": "测试桌1"
       }
     }
     ```
   - 说明部署成功！✅

4. **查看生成的小程序码**
   - 在 Web 控制台，点击：`云存储`
   - 进入 `qrcodes` 目录
   - 可以看到生成的小程序码图片

---

### 第七步：配置域名白名单（5分钟）

1. **获取云存储域名**
   - 在 Web 控制台，点击：`云存储`
   - 复制页面顶部的域名，例如：
     ```
     https://vkceyugu.cdn.bspapp.com
     ```

2. **配置小程序域名**
   - 登录微信公众平台：https://mp.weixin.qq.com
   - 进入：`开发` → `开发管理` → `开发设置`
   - 找到：`服务器域名`
   - 在 `downloadFile 合法域名` 中添加云存储域名
   - 点击：`保存`

---

## ✅ 验证部署

### 1. 在微信开发者工具中测试

1. **打开项目**
   - 在 HBuilderX 中，点击：`运行` → `运行到小程序模拟器` → `微信开发者工具`

2. **添加测试编译模式**
   - 在微信开发者工具中，点击：`编译模式` → `添加编译模式`
   - 模式名称：`测试扫码`
   - 启动参数：`scene=测试桌1`
   - 点击：`确定`

3. **测试扫码流程**
   - 点击：`编译`
   - 应该会弹出桌号确认弹窗
   - 显示：`桌号：测试桌1`
   - 选择人数，点击确认
   - 功能正常！✅

### 2. 生成真实小程序码测试

1. **在云函数测试中生成**
   - 生成几个测试桌号：`A01`, `A02`, `B01`

2. **下载小程序码**
   - 在云存储中下载生成的小程序码图片

3. **使用微信扫码**
   - 用微信扫描下载的小程序码
   - 应该会打开小程序并显示桌号确认弹窗
   - 完美！✅

---

## 🎉 部署完成

恭喜！你已经成功部署了扫码点餐的云端功能。

### 现在你可以：

1. ✅ 生成真实的小程序码
2. ✅ 测试完整的扫码流程
3. ✅ 在真机上测试功能
4. ✅ 开始开发管理后台

---

## 📊 部署检查清单

- [ ] uniCloud 服务空间已创建
- [ ] 微信凭证已配置（uni-open-bridge 或环境变量）
- [ ] 数据库集合已创建（zhuohao, token_cache, error_logs）
- [ ] 数据库索引已设置（table_number, cache_key）
- [ ] 云存储目录已创建（qrcodes）
- [ ] 云对象已上传（zhuohao）
- [ ] 云对象测试通过
- [ ] 域名白名单已配置
- [ ] 小程序中测试通过
- [ ] 真机扫码测试通过

---

## 🔧 常见问题

### 1. 上传云对象失败

**问题**：提示 "上传失败" 或 "网络错误"

**解决**：
- 检查网络连接
- 确认已登录 DCloud 账号
- 确认云服务空间已创建并绑定
- 重启 HBuilderX 后重试

### 2. 生成小程序码失败

**问题**：返回 `errCode: 2001` 或 "微信 API 调用失败"

**解决**：
- 检查微信凭证是否正确配置
- 确认 AppID 和 AppSecret 是否正确
- 检查 uni-open-bridge 配置格式
- 查看云函数日志获取详细错误信息

### 3. 小程序码无法显示

**问题**：生成成功但图片无法显示

**解决**：
- 检查域名白名单是否配置
- 确认云存储域名是否正确
- 在微信开发者工具中查看网络请求
- 检查图片 URL 是否可访问

### 4. 数据库操作失败

**问题**：返回 `errCode: 3001` 或 "数据库错误"

**解决**：
- 检查数据库集合是否创建
- 确认索引是否设置正确
- 查看云函数日志
- 检查数据库权限设置

### 5. Token 缓存问题

**问题**：频繁提示 "获取 access_token 失败"

**解决**：
- 检查 token_cache 集合是否创建
- 确认微信凭证是否正确
- 手动调用 `cleanExpiredTokenCache` 清理缓存
- 查看云函数日志

---

## 📞 获取帮助

### 查看日志
1. 在 Web 控制台，点击：`云函数/云对象`
2. 点击 `zhuohao` 云对象
3. 点击：`日志`
4. 查看详细的错误信息

### 查看文档
- [扫码点餐部署指南.md](私房菜点餐项目前端模版（微信小程序+H5）/扫码点餐部署指南.md) - 完整部署文档
- [扫码点餐使用指南.md](私房菜点餐项目前端模版（微信小程序+H5）/扫码点餐使用指南.md) - 使用说明
- [扫码点餐测试数据准备.md](私房菜点餐项目前端模版（微信小程序+H5）/扫码点餐测试数据准备.md) - 测试指南

---

## 🎯 下一步

部署完成后，你可以：

1. **开发管理后台**
   - 创建桌号管理页面
   - 实现小程序码生成界面
   - 实现批量操作功能

2. **完善测试**
   - 准备测试数据
   - 执行功能测试
   - 真机测试

3. **优化功能**
   - 添加批量生成功能
   - 实现小程序码下载
   - 添加打印功能

---

**文档版本：** v1.0.0  
**最后更新：** 2026年3月4日  
**适用版本：** HBuilderX 3.0+, uniCloud 阿里云版

