# 手机号快捷登录实现说明

## 前端实现

### 修改内容
已将登录页面改为手机号快捷登录模式：
- 移除了头像和昵称输入
- 使用微信官方的 `getPhoneNumber` API
- 一键登录，无需输入密码

### 实现原理

1. **用户点击登录按钮**
   - button 组件设置 `open-type="getPhoneNumber"`
   - 触发微信授权弹窗

2. **获取手机号 code**
   - 用户同意后，`@getphonenumber` 事件返回 `code`
   - 这个 code 用于后端换取手机号

3. **获取登录 code**
   - 调用 `uni.login()` 获取登录 code
   - 这个 code 用于后端获取 openid

4. **发送到后端**
   - 将两个 code 发送给后端
   - 后端解密获取手机号和 openid

## 后端需要实现的接口

### 1. 修改登录接口

后端需要接收两个参数：
```java
{
  "code": "登录code",  // 用于获取 openid
  "phoneCode": "手机号code"  // 用于获取手机号
}
```

### 2. 调用微信 API 获取手机号

后端需要调用微信接口：
```
POST https://api.weixin.qq.com/wxa/business/getuserphonenumber?access_token=ACCESS_TOKEN

请求参数：
{
  "code": "手机号code"
}

返回数据：
{
  "errcode": 0,
  "errmsg": "ok",
  "phone_info": {
    "phoneNumber": "13800138000",  // 用户绑定的手机号
    "purePhoneNumber": "13800138000",  // 没有区号的手机号
    "countryCode": "86",  // 区号
    "watermark": {
      "timestamp": 1637744274,
      "appid": "xxxx"
    }
  }
}
```

### 3. 后端实现步骤

```java
// 1. 使用登录 code 获取 openid 和 session_key
String openid = getOpenidByCode(loginCode);

// 2. 使用手机号 code 获取手机号
String phone = getPhoneNumberByCode(phoneCode);

// 3. 根据手机号查询或创建用户
Member member = memberService.findOrCreateByPhone(phone, openid);

// 4. 生成 token
String token = generateToken(member);

// 5. 返回登录信息
return {
  "code": 200,
  "data": {
    "token": token,
    "member": {
      "id": member.getId(),
      "phone": member.getPhone(),
      "nickname": member.getNickname() || member.getPhone(),
      "avatar": member.getAvatar() || defaultAvatar,
      "points": member.getPoints(),
      "balance": member.getBalance()
    }
  }
}
```

## 微信小程序配置

### 1. 开通手机号权限
- 登录微信公众平台
- 进入小程序管理后台
- 开发 -> 接口设置 -> 手机号快速验证
- 开通该功能

### 2. 注意事项
- 仅对非个人主体且已认证的小程序开放
- 从 2023年8月28日 起需要付费使用
- 标准单价：每次成功调用 0.03 元
- 每个小程序有 1000 次免费体验额度

### 3. 免费规则
以下类型小程序免费：
- 政府、非营利组织
- 事业单位（政务民生类目）
- 公立医疗机构
- 学历教育（学校）

## 后端示例代码

### Java Spring Boot 实现

```java
@PostMapping("/wechat/login")
public Result<LoginResponse> wechatLogin(@RequestBody LoginRequest request) {
    try {
        // 1. 获取 openid
        String openid = wechatService.getOpenid(request.getCode());
        
        // 2. 获取手机号
        String phone = null;
        if (request.getPhoneCode() != null) {
            phone = wechatService.getPhoneNumber(request.getPhoneCode());
        }
        
        // 3. 查询或创建用户
        Member member = memberService.findOrCreateByPhone(phone, openid);
        
        // 4. 生成 token
        String token = jwtService.generateToken(member.getId());
        
        // 5. 返回结果
        LoginResponse response = new LoginResponse();
        response.setToken(token);
        response.setMember(member);
        
        return Result.success(response);
    } catch (Exception e) {
        log.error("登录失败", e);
        return Result.error("登录失败：" + e.getMessage());
    }
}
```

### 获取手机号方法

```java
public String getPhoneNumber(String code) {
    // 1. 获取 access_token
    String accessToken = getAccessToken();
    
    // 2. 调用微信接口
    String url = "https://api.weixin.qq.com/wxa/business/getuserphonenumber?access_token=" + accessToken;
    
    Map<String, String> params = new HashMap<>();
    params.put("code", code);
    
    String response = httpClient.post(url, params);
    
    // 3. 解析返回结果
    JSONObject json = JSON.parseObject(response);
    if (json.getInteger("errcode") == 0) {
        JSONObject phoneInfo = json.getJSONObject("phone_info");
        return phoneInfo.getString("phoneNumber");
    } else {
        throw new RuntimeException("获取手机号失败：" + json.getString("errmsg"));
    }
}
```

## 测试步骤

### 1. 前端测试
1. 清除小程序缓存
2. 重新编译运行
3. 进入登录页面
4. 点击"手机号快捷登录"按钮
5. 在弹窗中选择手机号并确认

### 2. 后端测试
1. 确保后端已实现手机号解密接口
2. 配置微信小程序的 AppID 和 AppSecret
3. 测试接口是否能正确获取手机号
4. 验证用户创建和登录流程

## 常见问题

### 1. 获取不到手机号
- 检查小程序是否已开通手机号权限
- 检查是否已认证（个人小程序无法使用）
- 检查是否有足够的调用额度

### 2. code 无效
- code 只能使用一次
- code 有效期为 5 分钟
- 不要混用登录 code 和手机号 code

### 3. 后端解密失败
- 检查 access_token 是否有效
- 检查 AppID 和 AppSecret 是否正确
- 检查网络是否能访问微信 API

## 优势

1. **用户体验好**
   - 无需输入密码
   - 一键快速登录
   - 减少注册流程

2. **安全性高**
   - 微信官方验证
   - 手机号真实有效
   - 防止恶意注册

3. **开发简单**
   - 前端只需一个按钮
   - 后端调用微信 API
   - 无需短信验证码

## 注意事项

1. **合理使用**
   - 不要在不必要的场景强制要求手机号
   - 避免影响用户体验
   - 遵守微信平台规范

2. **成本控制**
   - 注意调用次数
   - 购买合适的资源包
   - 监控使用情况

3. **隐私保护**
   - 妥善保管用户手机号
   - 遵守隐私政策
   - 不得泄露或滥用
