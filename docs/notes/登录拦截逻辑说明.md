# 登录拦截逻辑说明

## 实现逻辑

### 1. 应用启动时检查（App.vue）
```javascript
onLaunch() {
  // 检查是否已登录
  if (!isLoggedIn()) {
    // 未登录，跳转到登录页
    uni.reLaunch({
      url: '/pages/login/login'
    })
  }
}
```

### 2. 首页检查（index.vue）
```javascript
onLoad() {
  // 检查登录状态
  if (!isLoggedIn()) {
    uni.reLaunch({
      url: '/pages/login/login'
    })
    return
  }
  // 继续加载页面
}

onShow() {
  // 每次显示时也检查
  if (!isLoggedIn()) {
    uni.reLaunch({
      url: '/pages/login/login'
    })
    return
  }
}
```

## 流程说明

### 首次打开小程序
1. App.vue 的 onLaunch 执行
2. 检查 token 是否存在
3. 如果不存在，跳转到登录页
4. 用户完成登录后，跳转到首页

### 已登录用户打开小程序
1. App.vue 的 onLaunch 执行
2. 检查 token 存在
3. 直接进入首页
4. 首页 onLoad 再次检查（双重保险）

### Token 过期处理
1. 用户操作时，后端返回 401
2. 前端拦截器捕获
3. 清除本地 token
4. 跳转到登录页

## 登录状态判断

### isLoggedIn() 函数
```javascript
export function isLoggedIn() {
  return !!getToken()
}
```

简单判断：只要本地有 token 就认为已登录

### Token 验证
- 前端不验证 token 是否过期
- 由后端验证 token 有效性
- 如果 token 无效，后端返回 401
- 前端拦截器处理 401 响应

## 页面跳转方式

### uni.reLaunch()
- 关闭所有页面，打开新页面
- 不能返回到之前的页面
- 适合登录/登出场景

### 为什么用 reLaunch 而不是 redirectTo
- reLaunch 会清空页面栈
- 防止用户通过返回键回到未登录状态的页面
- 确保登录后是全新的页面状态

## 需要登录拦截的页面

### 必须登录的页面
- 首页（index）
- 点餐页（menu）
- 订单页（order）
- 我的页面（my）
- 所有需要用户信息的页面

### 不需要登录的页面
- 登录页（login）
- 可能的活动页、广告页等

## 后端配合

### 1. Token 生成
```java
// 登录成功后生成 token
String token = JWT.create()
    .withClaim("userId", user.getId())
    .withExpiresAt(new Date(System.currentTimeMillis() + 7 * 24 * 60 * 60 * 1000)) // 7天
    .sign(Algorithm.HMAC256(secret));
```

### 2. Token 验证
```java
@Component
public class AuthInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, 
                           HttpServletResponse response, 
                           Object handler) {
        String token = request.getHeader("token");
        
        if (token == null || !isValidToken(token)) {
            response.setStatus(401);
            return false;
        }
        
        return true;
    }
}
```

### 3. 返回 401 时的响应
```json
{
  "code": 401,
  "msg": "未登录或登录已过期",
  "data": null
}
```

## 前端请求拦截器

### request.js 中添加 401 处理
```javascript
uni.request({
  // ...
  success: (res) => {
    if (res.statusCode === 401) {
      // Token 过期或无效
      clearAuth()
      uni.showToast({
        title: '登录已过期，请重新登录',
        icon: 'none'
      })
      setTimeout(() => {
        uni.reLaunch({
          url: '/pages/login/login'
        })
      }, 1500)
      reject(res)
      return
    }
    // 其他处理...
  }
})
```

## 测试场景

### 1. 首次打开
- 清除小程序缓存
- 打开小程序
- 应该直接进入登录页

### 2. 已登录打开
- 登录成功后
- 关闭小程序
- 重新打开
- 应该直接进入首页

### 3. Token 过期
- 修改后端 token 有效期为 1 分钟
- 登录后等待 1 分钟
- 进行任何操作
- 应该提示登录过期并跳转到登录页

### 4. 手动清除 token
- 在开发者工具中清除 storage
- 刷新页面
- 应该跳转到登录页

## 注意事项

### 1. 避免循环跳转
- 登录页不要检查登录状态
- 使用 reLaunch 而不是 navigateTo

### 2. 页面栈管理
- 登录成功后使用 switchTab 或 reLaunch
- 不要使用 navigateTo，避免页面栈过深

### 3. Token 存储
- 使用 uni.setStorageSync 同步存储
- 确保 token 持久化
- 小程序关闭后 token 仍然存在

### 4. 多端兼容
- H5 端使用 localStorage
- 小程序端使用 storage
- 统一使用 uni.setStorageSync API

## 优化建议

### 1. Token 刷新机制
- 实现 refresh token
- Token 快过期时自动刷新
- 避免用户频繁登录

### 2. 静默登录
- 如果 token 过期但 session_key 有效
- 可以静默重新登录
- 提升用户体验

### 3. 登录状态同步
- 多个 tab 页面之间同步登录状态
- 使用 uni.$on 和 uni.$emit
- 或使用 Vuex 管理全局状态

## 完整流程图

```
打开小程序
    ↓
App.onLaunch
    ↓
检查 token
    ↓
  有 token ────→ 进入首页 ────→ 首页 onLoad 再次检查
    ↓                              ↓
  无 token                      有 token → 正常使用
    ↓                              ↓
跳转登录页                      无 token → 跳转登录页
    ↓
用户登录
    ↓
保存 token
    ↓
跳转首页
```
